# Desktop-specific Hyprland overrides
# Symlinked to ~/.config/hypr/machine.conf by: dots install --machine desktop

# DDC brightness on the focused monitor, with cached i2c bus (fast)
bindel = , XF86MonBrightnessUp,   exec, dots monitor brightness +5
bindel = , XF86MonBrightnessDown, exec, dots monitor brightness -5
