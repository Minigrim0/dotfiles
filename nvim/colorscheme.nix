# Colorscheme configuration ported from your colorscheme.lua
{
  programs.nixvim.colorschemes.gruvbox = {
    enable = true;
    settings = {
      # From your gruvbox-material config
      background = "hard";
      better_performance = true;
    };
  };
}